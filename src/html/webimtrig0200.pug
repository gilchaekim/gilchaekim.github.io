extends ../components/templates/base.pug

block prepend top 
  - mainClassName="webauc"
  - pageTitle = "즉시이체"

block content
  +content_group(1)
    
    +title({
      text:"출금계좌",
      lv:4,
      className:"min_title"
    })
    +button_select_account({
      isMoney:true
    })
  +content_group(1)
    +content_group(3)
      button.total_data
        span 
          | 총 
          em 2
          | 건
        span 
          em 80000000
          | 원
    +content_group(3)
      +form()
        +form_body({title:"받는 분"})
          +button_select({
            placeholder:"은행 선택",
            label:"한화저축은행"
          })
          +input({
            value:"000-000-00000",
            placeholder:"계좌번호 입력"
          })
          +button_custom({
            type:"timeselect-24",
            label:"최근ㆍ자주ㆍ내계좌"
          })
        +form_body({title:"보낼 금액"})
          +input({
            placeholder:"숫자만 입력"
          })
          .money_support
            span 1,000만 원
          +button_custom({
            type:"white-round-24",
            label:"이체한도",
          })
        +form_body({title:"혼합식적금 입금 구분"})
          +button_select({
            placeholder:"대상조회"
          })
          //- +button({
          //-   label:"대상조회",
          //-   size:"medium",
          //-   type:"white",
          //- })
          //- +radioCheck({
          //-   type:"radio",
          //-   name:'chk687565',
          //-   lists:[
          //-     {label:'약정부금액',checked:true},
          //-     {label:'추가부금액',},
          //-   ]
          //- })
  +content_group(0)
    +collapse({
      isOpen:true,
      className:"plus_data"
    })
      +header()
        p.acc_title [선택] 추가정보
      +content()
        .acc_data2
          +form()
            +form_body({title:"받는 계좌에 표시"})
              +input({
                value:"김중앙(저축은행)",
                maxlength:10,
              })
            +form_body({title:"내 계좌에 표시 "})
              +input({
                placeholder:"10자 이내 입력",
                maxlength:10,
              })

 
  +button_bottom_fixed()
    +button({
      label:"다음",
      size:"large",
    })

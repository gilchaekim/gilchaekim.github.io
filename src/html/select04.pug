extends ../components/templates/base.pug




block content
  +button({
    label:"체크 항목이 있는 리스트",
    muiToggle:'target:#modal_bottom;'
  })

  +modal({
    title:"리스트 제목",
    id:"modal_bottom",
    type:"bottom"
  })
    +select_list({className:"check_select"})
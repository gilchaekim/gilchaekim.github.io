extends ../components/templates/base.pug

block prepend top 
  - mainClassName="webinf"
  - pageTitle = "약관동의"

block content
  +stepNum({
    total:6,
    activeIndex:2
  })
  +content_group(1)
    +title({
      text:'약관동의',
      lv:2,
    })
  
  +content_group(2)
    +agree_check({
      text:"모두 동의합니다.<p class='sub_txt'>고객 편의 제공을 위하여 여러 동의사항에 대한 전체동의 기능을 제공하고 있습니다. 전체동의 시 선택 동의사항이 포함될 수 있습니다."
    })
      +agree_check({
        text:"[필수] 개인(신용)정보 수집 이용 제공 동의"
      })
      +agree_check({
        text:"[필수] 개인(신용)정보조회 동의"
      })
      +agree_check({
        text:"[필수] 개인(신용)정보 제3자 제공 동의"
      })
      +agree_check({
        text:"[필수] 개인(신용)정보 수집 이용 제공 동의서"
      })
      +agree_check({
        text:"[필수] 개인(신용)정보 조회 동의서"
      })
      +agree_check({
        text:"[선택] 개인(신용)정보 수집이용 · 동의서(보증료 우대 서비스)"
      })
      +agree_check({
        text:"[선택] 개인(신용)정보 수집이용 · 동의서(상품서비스 안내 등)"
      })
  +button_bottom_fixed()
    +button({
      label:"다음",
      size:"large",
      type:"primary",
      disabled:"disabled"
    })
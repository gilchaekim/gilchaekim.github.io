extends ../components/templates/base.pug

block prepend top 
  - mainClassName="webinf"
  - pageTitle = "전자약정"

block content
  +content_group(0)
    +title({
      text:"실명인증 및 인증서 인증",
      lv:2,
    })
    .text_wrap
      +text({
        text:"개인정보보호를 위해 인증서로 본인확인을 진행합니다."
      })
  +content_group(2)
    +form()
      +form_body({title:"이름"})
        +input({
          placeholder:"이름 입력"
        })
      +form_body({title:"주민등록번호"})
        +input_idnumber()
    
  +button_bottom_fixed()
    +button({
      label:"다음",
      size:"large",
    })